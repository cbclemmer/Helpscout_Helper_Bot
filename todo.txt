Watcher will not work becauese when you create a new note in the thread the sha changes so it will continue making notes, thus making the sha change every time
Something in the formatting in the completions are making the '###CONVERSATION' string appear in the completion
Something in the formatting in the completions are making the completion return multiple '###THREAD' in the completion
Add "@ScoutBot summarize last" to summarize the last customer message
Add more length to response
Add vector search for related issues
Change print statements to log file, pass log file descriptor around
